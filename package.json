{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn --cwd packages/sendou-ink-web dev",
    "build": "yarn --cwd packages/sendou-ink-web build",
    "start": "yarn --cwd packages/sendou-ink-web start"
  }
}
